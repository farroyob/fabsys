import 'package:flutter/material.dart';

import 'home_page.dart';

class MyApp extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    Widget  vloWidget ;

    vloWidget = new MaterialApp(

    );

    return MaterialApp(
        home: Center ( child : HomePage()
        )
    ) ;
  }

}